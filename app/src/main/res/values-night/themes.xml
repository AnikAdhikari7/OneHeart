<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.OneHeart" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:statusBarColor">@color/lightGreen</item>
        <item name="android:colorPrimary">@color/lightGreen</item>
        <item name="colorSecondary">@color/lightBlue</item>
    </style>

    <style name="Theme.OneHeart" parent="Base.Theme.OneHeart" />
</resources>